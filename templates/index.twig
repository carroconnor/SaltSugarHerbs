<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ siteName }}</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Bentham&family=Sacramento&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ siteUrl }}/assets/style.css">
</head>
<body>
    {# Classic HTML & CSS page. Can use twig language throughout and reference blocs #}
    <header class="header">
        <div class="headerImage">
            <img src="{{ siteUrl }}assets/Images/HeaderImage.jpg" alt="Louisville Skyline">
        </div>

        <nav class="nav">
            <div class="homePageTitle"> 
                {% block titleSection %}
                {% endblock %}
            </div>
            <div id="rightNav">
                {% set slug = craft.app.request.getSegment(1)%}
                {% for link in header.navigation.all() %}
                    {% set destination = link.linkDestination.one() %}

                    <a href="{{ destination.url }}" class="nav-link {% if slug == destination.slug %}nav-link--active{% endif %}">{{ link.linkText }}</a>
                {% endfor %}
            </div>
        </nav>
        <div class="headerDescription">
                {% block headerLandingContent %}
                {% endblock %}
        <div class="button">
        {# redo buttons #}
            <a href="https://www.airbnb.com/"><p class="buttonText">Book Now</p>
        </a>
        </div>
    </div>
    </header>
    {# Templates Inserted here #}

    <p>Where does this go?</p>
    
    <div class="homeGrid flex">
        <div class="left-column">
            <div class="blog-title">
            {% block blogTitleContent %}
                {# blog title/date #}
            {% endblock %}
            </div>
            
            {% block content %}
                {# Page template inserted here #}
            {% endblock %}
                   
        </div>

        <div class="right-column">
            {% block rightContent %}
                {# Page template inserted here #}
            {% endblock %}
        </div>
    </div>

    <div class="spotlightSection">
    <h2>
        <span>
            <strong class="spotLightText">Spotlight/</strong>
                <a href="blog" class="newestPostsText">
                    newest posts</a>
        </span>

        </br>
    </h2>
        <div class="spotlightContent flex">
        {% block spotlightContent %}
            {# Recent posts here #}
        {% endblock %}
        </div>

        <div class="spotlightContent flex">
        {% block secondRowSpotlight %}
            {# Recent posts here #}
        {% endblock %}
        </div>
    <div>
    <footer class="footer">
        {{ footer.richText }}
    </footer>

    <script src="{{ siteUrl }}assets/script.js"></script>
    
</body>
</html>